# Docker Engine 29.x enforces a minimum API version of 1.40 and rejects docker-java's
# hardcoded default of 1.32 with an empty 400, which kills Testcontainers' Docker detection.
# Pin a version every supported daemon accepts (1.44 = Docker 25+, Feb 2024).
api.version=1.44
